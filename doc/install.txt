COMPILACIÓN APLICACIÓN
----------------------------


Se puede compilar la aplicación de la siguiente manera: mvn -DskipTests clean install

Copiar el ear generado en la carpeta ${jboss.home}/standalone/deployments/
